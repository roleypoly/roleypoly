load("//:hack/react.bzl", "react_library")

package(default_visibility = ["//visibility:public"])

react_library(
    name = "key-events",
    deps = [
        "react",
        "@types/react",
    ],
)
